CREATE EXTERNAL TABLE `ads_sv_material_grade_data_result_external` (
  `dt` date NOT NULL COMMENT "分区日期",
  `material_id` bigint(20) NOT NULL COMMENT "素材任务id",
  `date_key` date NULL COMMENT "投放日期",
  `material_type` varchar(255) NULL COMMENT "素材类型",
  `source_chl` varchar(255) NULL COMMENT "媒体",
  `language_name` varchar(255) NULL COMMENT "语言",
  `code` varchar(255) NULL COMMENT "短剧code",
  `spend` decimal(16, 6) NULL COMMENT "当日花费",
  `impressions` int(11) NULL COMMENT "当日展示量",
  `clicks` int(11) NULL COMMENT "当日点击互动",
  `link_clicks` int(11) NULL COMMENT "当日链接点击",
  `installs` int(11) NULL COMMENT "当日激活",
  `amount` decimal(16, 6) NULL COMMENT "当日H24收入",
  `first_date` date NULL COMMENT "首次投放日期",
  `grade_date` date NULL COMMENT "首次日花费>50的日期，null说明没有",
  `rn` int(11) NULL COMMENT "没用",
  `spend_sum` decimal(16, 6) NULL COMMENT "截止当前累计花费",
  `impressions_sum` int(11) NULL COMMENT "截止当前累计展示量",
  `clicks_sum` int(11) NULL COMMENT "截止当前累计点击互动",
  `link_clicks_sum` int(11) NULL COMMENT "截止当前累计链接点击",
  `installs_sum` int(11) NULL COMMENT "截止当前累计激活",
  `amount_sum` decimal(16, 6) NULL COMMENT "截止当前累计H24收入",
  `ctr_link` decimal(16, 6) NULL COMMENT "当日链接ctr",
  `cvr` decimal(16, 6) NULL COMMENT "当日cvr",
  `roas` decimal(16, 6) NULL COMMENT "当日roas",
  `cpm` decimal(16, 6) NULL COMMENT "当日cpm",
  `ctr_link_sum` decimal(16, 6) NULL COMMENT "累计链接ctr",
  `cvr_sum` decimal(16, 6) NULL COMMENT "累计cvr",
  `roas_sum` decimal(16, 6) NULL COMMENT "累计roas",
  `cpm_sum` decimal(16, 6) NULL COMMENT "累计cpm",
  `ctr_to1` decimal(16, 6) NULL COMMENT "当日链接ctr，归一处理",
  `cvr_to1` decimal(16, 6) NULL COMMENT "当日cvr，归一处理",
  `roas_to1` decimal(16, 6) NULL COMMENT "当日roas，归一处理",
  `grade_score` decimal(16, 6) NULL COMMENT "评级得分，满分是5分",
  `grade` varchar(255) NULL COMMENT "分级",
  `etl_tm` datetime NULL COMMENT "清洗时间"
) ENGINE=OLAP_EXTERNAL 
PRIMARY KEY(`dt`, `material_id`)
COMMENT "海剧素材分级表外部表"
DISTRIBUTED BY HASH(`dt`, `material_id`) BUCKETS 3 
PROPERTIES (
"replication_num" = "3",
"in_memory" = "false",
"enable_persistent_index" = "true",
"replicated_storage" = "true",
"storage_medium" = "SSD",
"compression" = "ZSTD",
"host" = "ads-starrocks-in.changdu.vip",
"port" = "19020",
"user" = "ztt",
"password" = "",
"database" = "ads",
"table" = "ads_sv_material_grade_data_result"
);