# Plan 模板

Program 进入方案设计阶段时，Agent 按此模板与用户对话，确认后写入 `workspace/PLAN.md`。

根据 `type` 字段选取对应章节：`development` 使用 1-7 章；`analysis` 使用 1、4-8 章（2、3 不适用）。

---

## 1. 问题陈述

> 一句话描述本次要解决的问题或实现的目标。

| 属性 | 值 |
|------|----|
| Program 类型 | {development / analysis} |
| 分析子类型 | {仅 analysis：batch / deep-dive / ad-hoc} |

---

## 2. 表分析（开发类）

### 2.1 目标表

| 属性 | 值 |
|------|----|
| 分层 | {ods / dwd / dws / dim / ads} |
| 表名 | {schema}.{table_name} |
| 操作类型 | {新建 / 新增字段 / 修改口径 / 重构} |
| 粒度 | {日 di / 小时 hi / 全量 df / 增量 ed} |
| 业务域 | {业务域名称} |

### 2.2 上游依赖

| 上游表 | 用途 | 是否有 DDL 可参考 |
|--------|------|-------------------|
| {schema}.{table} | {说明} | {是/否} |

### 2.3 下游影响

| 下游表 | 影响方式 | 风险等级 |
|--------|----------|----------|
| {schema}.{table} | {说明} | {高/中/低} |

---

## 3. 字段变更（开发类）

### 新增字段（适用于新表或 ALTER）

| 字段名 | 类型 | 来源表/计算逻辑 | 备注 |
|--------|------|----------------|------|
| dt | date | `${dt}` | |
| {field} | {type} | {source} | |

### 口径变更（如适用）

| 字段名 | 旧口径 | 新口径 | 变更原因 |
|--------|--------|--------|----------|
| {field} | {旧来源} | {新来源} | {原因} |

---

## 4. 技术要点 / 方法论

### 关键设计决策

- {决策 1}
- {决策 2}

### 开发类
- Join 逻辑：关联键 {key}，粒度检查 {是否已确认维表唯一}
- 数据质量措施：NULL 兜底 {说明}，脏数据过滤 {说明}

### 分析类
- 分析步骤：数据采集 → 小样验证 → 分批执行 → 汇总
- 分批策略：每批 {M} 个对象，中间汇报
- 边界情况处理策略：{标记上报 / 默认规则 / 跳过}

---

## 5. 回刷评估（开发类）

| 项目 | 说明 |
|------|------|
| 是否需要回刷 | {是/否} |
| 回刷范围 | {日期范围} |
| 风险提示 | {如有} |

---

## 6. 验证计划

### 开发类

```sql
-- 1. 数据量级检查
SELECT dt, count(*) from {schema}.{table} group by dt order by dt desc limit 7
;

-- 2. 枚举值一致性
select distinct {col1}, {col2} from {schema}.{table} where dt = '${dt}'
;

-- 3. 指标对比（如有口径变更）
select dt, key_col, old_metric, new_metric
from old_table a join new_table b on ...
where a.metric <> b.metric
;
```

### 分析类

| 检查项 | 方法 | 通过标准 |
|--------|------|----------|
| 覆盖率 | 分析对象数 vs 范围预估数 | ≥ 95% |
| 一致性 | 同类对象结论交叉比对 | 无矛盾 |
| 可复现性 | 审查结论→依据链 | 每个结论有明确依据 |
| 边界处理 | 统计 exceptions 记录数 | 全部标记并上报 |

---

## 7. 分析范围 & 评估框架（分析类）

### 7.1 分析对象

| 维度 | 说明 |
|------|------|
| 目标分层/ Schema | {ods / dwd / dws / ads / dim / 全部} |
| 业务域 | {全部 / 阅读 / 短剧 / 圣经 / 广告 / ...} |
| 对象类型 | {表 / 视图 / ETL 任务 / 字段 / ...} |
| 筛选条件 | {排除 _bak _tmp _test 等} |
| 预估对象数 | {N} |

### 7.2 排除条件

| 排除项 | 原因 |
|--------|------|
| {模式/条件} | {为什么排除} |

### 7.3 评估框架

| 规则来源 | 说明 |
|----------|------|
| `SHARED/knowledge/{规则文件}` | {简述} |
| 用户本次定义 | {简述} |

### 7.4 分析维度

| 维度 | 说明 | 判定规则 |
|------|------|----------|
| {维度1} | {说明} | {具体判定条件} |
| {维度2} | {说明} | {具体判定条件} |

### 7.5 数据采集方案

| 数据项 | 来源 | 采集方式 |
|--------|------|----------|
| 表清单 | `starrocks/{layer}/` 目录扫描 | Glob / Grep |
| 字段信息 | DDL 文件 | Read |
| 存储量级 | StarRocks 查询 | Python 脚本 |
| {其他} | {来源} | {方式} |

### 7.6 产出物规格

| 产出物 | 路径 | 格式 |
|--------|------|------|
| 分析报告 | `workspace/report.md` | Markdown |
| 结构化结果 | `workspace/output/{name}.csv` | CSV |
| 仓库文档变更 | `SHARED/knowledge/{file}` | YAML/MD |

---

## 8. 待确认问题

| # | 问题 | 选项 | 用户决策 |
|---|------|------|----------|
| 1 | {具体问题} | {A / B / C} | |
| 2 | {具体问题} | {A / B / C} | |
