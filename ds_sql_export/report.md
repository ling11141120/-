# DolphinScheduler SQL 节点导出 V2 报告

## 导出统计

- 项目过滤: starrocks, data_quality
- 上线工作流数: 685
- SQL 节点数: 1184
- 输出 SQL 文件数: 835
- 已映射 SQL 段数: 1615
- 异常数: 46
- 多目标异常数: 15
- 多目标节点数: 15
- 多目标语句数: 1

## 口径说明

- 文件按目标表聚合命名为 `P_{table_name}.sql`。
- 同一目标表多个 SQL 节点追加到同一个文件，每段 SQL 前保留 DS 元数据头。
- 一个 DS SQL 节点内多条写入语句会按目标表分别写入对应文件。
- 已映射 SQL 段数按成功导出的语句段统计，异常数不包含多目标审计清单。
- `ods_` 与 `ods_log_` 均归入 `ods` 目录。
- `das_` 归入 `das` 目录。
