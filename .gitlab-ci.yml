stages:
  - deploy

generate_json:
  stage: deploy
  script:
    #- sh -x test.sh
    - for i in ${MY_ARRAY[@]}; do
        echo "Building $i";
        # Do something with $i
      done  
      
      
  artifacts:
    paths:
      - changed_files.txt
  only:
    - stage
  tags:
    - kunlun-runner